# Conflux Composer Coordinate Scaffold

This repository contains only metadata used to reserve the Composer package coordinate conflux/conflux for the Josh Mouch-owned Conflux project family.

It contains no functional implementation. A future real package may replace this scaffold only through the owning project's release process.
